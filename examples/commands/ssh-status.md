---
description: Check SSH session status
---

Check the current SSH session status using the `ssh_info` tool. If connected, also run `hostname` and `uptime` on the remote host to confirm the connection is working.
