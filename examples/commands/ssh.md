---
description: Connect to a remote host via SSH
---

Connect to the SSH host `$ARGUMENTS` using the `ssh_connect` tool. Once connected, let me know the connection status using `ssh_info`.

If no host is provided, ask me which host to connect to.
